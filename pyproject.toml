[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "mp3-mp4-downloader"
version = "1.0.0"
description = "MP3/MP4 Downloader Web App"
requires-python = ">=3.9"
dependencies = [
    "streamlit>=1.28.0",
    "yt-dlp>=2024.1.0",
]
